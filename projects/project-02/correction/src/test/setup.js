// Vitest global setup — runs once before the test suite.
// Adds the jest-dom matchers (toBeInTheDocument, toHaveClass, ...)
// so they are available in every test file.
import '@testing-library/jest-dom';
